"use client";

import { createLayoutPart } from "../core";

export const RealEstatePortfolioLayout = createLayoutPart({
  "title": "Property portfolio",
  "description": "Listings, occupancy, tenants, maintenance, and revenue.",
  "style": "portal"
});
