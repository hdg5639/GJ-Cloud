"use client";

import { createLayoutPart } from "../core";

export const LogisticsDispatchLayout = createLayoutPart({
  "title": "Dispatch control",
  "description": "Plan routes, monitor stops, and resolve delivery exceptions.",
  "style": "map"
});
