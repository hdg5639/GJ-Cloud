"use client";

import { createLayoutPart } from "../core";

export const BillingWorkspaceLayout = createLayoutPart({
  "title": "Billing workspace",
  "description": "Subscriptions, invoices, collections, and revenue operations.",
  "style": "ledger"
});
