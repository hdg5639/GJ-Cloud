"use client";

import { createLayoutPart } from "../core";

export const LegalCaseLayout = createLayoutPart({
  "title": "Legal matter workspace",
  "description": "Matters, documents, deadlines, holds, and approvals.",
  "style": "split"
});
