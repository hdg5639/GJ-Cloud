"use client";

import { createLayoutPart } from "../core";

export const AiStudioLayout = createLayoutPart({
  "title": "AI studio",
  "description": "Models, prompts, evaluations, deployments, and experiments.",
  "style": "studio"
});
