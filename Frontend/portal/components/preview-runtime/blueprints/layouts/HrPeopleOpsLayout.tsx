"use client";

import { createLayoutPart } from "../core";

export const HrPeopleOpsLayout = createLayoutPart({
  "title": "People operations",
  "description": "Employee lifecycle, policies, requests, and workforce signals.",
  "style": "sidebar"
});
