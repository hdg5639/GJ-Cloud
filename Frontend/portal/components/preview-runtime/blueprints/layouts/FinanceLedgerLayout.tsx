"use client";

import { createLayoutPart } from "../core";

export const FinanceLedgerLayout = createLayoutPart({
  "title": "Finance ledger",
  "description": "Ledger-oriented view for cash movement and reconciliation.",
  "style": "ledger"
});
