"use client";

import { createLayoutPart } from "../core";

export const LearningPortalLayout = createLayoutPart({
  "title": "Learning portal",
  "description": "Course discovery, progress, assignments, and cohort activity.",
  "style": "portal"
});
