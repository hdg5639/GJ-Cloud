"use client";

import { createLayoutPart } from "../core";

export const InventoryWarehouseLayout = createLayoutPart({
  "title": "Warehouse operations",
  "description": "Coordinate stock, locations, replenishment, and transfers.",
  "style": "cockpit"
});
