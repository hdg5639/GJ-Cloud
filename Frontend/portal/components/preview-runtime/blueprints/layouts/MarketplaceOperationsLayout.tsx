"use client";

import { createLayoutPart } from "../core";

export const MarketplaceOperationsLayout = createLayoutPart({
  "title": "Marketplace operations",
  "description": "Vendors, listings, orders, disputes, and payouts.",
  "style": "sidebar"
});
