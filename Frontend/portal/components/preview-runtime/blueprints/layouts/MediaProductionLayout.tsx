"use client";

import { createLayoutPart } from "../core";

export const MediaProductionLayout = createLayoutPart({
  "title": "Media production",
  "description": "Assets, review, versions, publishing, and distribution.",
  "style": "studio"
});
