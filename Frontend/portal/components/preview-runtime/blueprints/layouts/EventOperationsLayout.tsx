"use client";

import { createLayoutPart } from "../core";

export const EventOperationsLayout = createLayoutPart({
  "title": "Event operations",
  "description": "Run event preparation, attendance, venues, and incidents.",
  "style": "planner"
});
