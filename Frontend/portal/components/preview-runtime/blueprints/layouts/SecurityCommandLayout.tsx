"use client";

import { createLayoutPart } from "../core";

export const SecurityCommandLayout = createLayoutPart({
  "title": "Security command center",
  "description": "Triage active threats, investigations, and response controls.",
  "style": "cockpit"
});
