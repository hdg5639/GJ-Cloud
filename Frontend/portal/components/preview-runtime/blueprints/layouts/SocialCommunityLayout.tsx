"use client";

import { createLayoutPart } from "../core";

export const SocialCommunityLayout = createLayoutPart({
  "title": "Community workspace",
  "description": "Feed, members, moderation, and engagement.",
  "style": "portal"
});
