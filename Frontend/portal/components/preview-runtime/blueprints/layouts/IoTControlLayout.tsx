"use client";

import { createLayoutPart } from "../core";

export const IoTControlLayout = createLayoutPart({
  "title": "IoT control center",
  "description": "Device fleet, telemetry, alerts, and remote commands.",
  "style": "cockpit"
});
