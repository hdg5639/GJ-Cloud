"use client";

import { createLayoutPart } from "../core";

export const KnowledgeBaseLayout = createLayoutPart({
  "title": "Knowledge workspace",
  "description": "Structured navigation, article content, and related references.",
  "style": "studio"
});
