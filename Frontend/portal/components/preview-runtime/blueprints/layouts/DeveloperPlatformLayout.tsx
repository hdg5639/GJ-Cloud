"use client";

import { createLayoutPart } from "../core";

export const DeveloperPlatformLayout = createLayoutPart({
  "title": "Developer platform",
  "description": "API products, environments, keys, usage, and releases.",
  "style": "console"
});
