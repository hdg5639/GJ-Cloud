"use client";

import { createLayoutPart } from "../core";

export const SupportConsoleLayout = createLayoutPart({
  "title": "Support console",
  "description": "Queue, customer context, and response workspace.",
  "style": "sidebar"
});
