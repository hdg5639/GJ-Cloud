"use client";

import { createLayoutPart } from "../core";

export const TravelPlannerLayout = createLayoutPart({
  "title": "Travel planner",
  "description": "Trips, itinerary, reservations, travelers, and disruptions.",
  "style": "planner"
});
