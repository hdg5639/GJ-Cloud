"use client";

import { createLayoutPart } from "../core";

export const BookingPlannerLayout = createLayoutPart({
  "title": "Booking planner",
  "description": "Calendar-first reservation and capacity workspace.",
  "style": "planner"
});
