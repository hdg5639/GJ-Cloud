import { BlueprintActionButtons, BlueprintKeyValueGrid, BlueprintSection, BlueprintStatusPill, formatBlueprintValue } from "../core";
import type { BlueprintAction, BlueprintField, BlueprintRecord, BlueprintTimelineEvent } from "../core";
import { TimelineCollection } from "../collections";

export function ContentArticleDetail({ article, metadata, actions, revisions, preview, onAction }: { article: BlueprintRecord; metadata: BlueprintField[]; actions: BlueprintAction[]; revisions?: BlueprintTimelineEvent[]; preview?: React.ReactNode; onAction?: (action: BlueprintAction) => void }) {
  return <div className="space-y-4"><header className="rounded-[18px] border border-line bg-panel p-5"><div className="flex flex-col gap-4 md:flex-row md:items-start md:justify-between"><div className="min-w-0"><p className="text-[10px] font-extrabold uppercase tracking-[0.18em] text-brand-strong">Content</p><div className="mt-2 flex flex-wrap items-center gap-3"><h2 className="max-w-3xl text-2xl font-black">{formatBlueprintValue(article.title ?? article.name)}</h2><BlueprintStatusPill value={article.status ?? "draft"} /></div><p className="mt-2 text-sm text-muted-soft">{formatBlueprintValue(article.excerpt ?? article.description ?? article.slug)}</p></div><BlueprintActionButtons actions={actions} onAction={onAction} /></div></header><div className="grid gap-4 xl:grid-cols-[minmax(0,1fr)_340px]"><div className="space-y-4"><BlueprintSection title="Preview" description="Rendered content preview.">{preview ?? <div className="prose prose-invert max-w-none text-sm leading-7 text-muted">{formatBlueprintValue(article.body ?? article.content ?? "No preview content")}</div>}</BlueprintSection>{revisions && <BlueprintSection title="Revision history"><TimelineCollection events={revisions} compact /></BlueprintSection>}</div><BlueprintSection title="Publishing metadata"><BlueprintKeyValueGrid fields={metadata} columns={1} /></BlueprintSection></div></div>;
}
