import { BlueprintSection, BlueprintStatusPill } from "../core";
import type { BlueprintPermission } from "../core";

export function SettingsDetail({ sections, permissions, onPermissionChange }: { sections: Array<{ id: string; title: string; description?: string; content: React.ReactNode; status?: string }>; permissions?: BlueprintPermission[]; onPermissionChange?: (permission: BlueprintPermission, enabled: boolean) => void }) {
  return <div className="space-y-4">{sections.map((section) => <BlueprintSection key={section.id} title={section.title} description={section.description} action={section.status ? <BlueprintStatusPill value={section.status} /> : undefined}>{section.content}</BlueprintSection>)}{permissions && <BlueprintSection title="Permissions" description="Fine-grained capability access for this configuration."><div className="divide-y divide-line">{permissions.map((permission) => <label key={permission.id} className="flex items-start justify-between gap-4 py-3 first:pt-0 last:pb-0"><div><strong className="text-sm">{permission.label}</strong>{permission.description && <p className="mt-1 text-xs leading-5 text-muted-soft">{permission.description}</p>}</div><input type="checkbox" checked={permission.enabled} disabled={permission.locked} onChange={(event) => onPermissionChange?.(permission, event.target.checked)} className="mt-1 h-4 w-4 accent-[var(--brand)]" /></label>)}</div></BlueprintSection>}</div>;
}
