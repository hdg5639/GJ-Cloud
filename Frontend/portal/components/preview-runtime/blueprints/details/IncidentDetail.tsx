import { BlueprintActionButtons, BlueprintKeyValueGrid, BlueprintMetricGrid, BlueprintSection, BlueprintStatusPill, formatBlueprintValue } from "../core";
import type { BlueprintAction, BlueprintField, BlueprintMetric, BlueprintRecord, BlueprintTimelineEvent } from "../core";
import { TimelineCollection } from "../collections";

export function IncidentDetail({ incident, metrics, fields, actions, timeline, runbook, onAction }: { incident: BlueprintRecord; metrics: BlueprintMetric[]; fields: BlueprintField[]; actions: BlueprintAction[]; timeline: BlueprintTimelineEvent[]; runbook?: React.ReactNode; onAction?: (action: BlueprintAction) => void }) {
  return <div className="space-y-4"><header className="rounded-[18px] border border-[color-mix(in_srgb,var(--preview-status-danger,#e0484d)_35%,var(--line))] bg-[color-mix(in_srgb,var(--preview-status-danger,#e0484d)_7%,var(--panel))] p-5"><div className="flex flex-col gap-4 md:flex-row md:items-start md:justify-between"><div><p className="text-[10px] font-extrabold uppercase tracking-[0.18em] text-[var(--preview-status-danger,#e0484d)]">Incident</p><div className="mt-2 flex flex-wrap items-center gap-3"><h2 className="text-2xl font-black">{formatBlueprintValue(incident.title ?? incident.name ?? incident.id)}</h2><BlueprintStatusPill value={incident.status ?? "open"} tone={incident.severity === "CRITICAL" ? "danger" : undefined} /></div><p className="mt-2 text-sm text-muted-soft">{formatBlueprintValue(incident.summary ?? incident.description)}</p></div><BlueprintActionButtons actions={actions} onAction={onAction} /></div></header><BlueprintMetricGrid metrics={metrics} columns={4} /><div className="grid gap-4 xl:grid-cols-[minmax(0,1fr)_360px]"><div className="space-y-4"><BlueprintSection title="Incident timeline"><TimelineCollection events={timeline} /></BlueprintSection>{runbook}</div><BlueprintSection title="Context"><BlueprintKeyValueGrid fields={fields} columns={1} /></BlueprintSection></div></div>;
}
