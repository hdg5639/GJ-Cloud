"use client";

import { createThemePart } from "../core";

export const SunsetOperationsTheme = createThemePart({
  "id": "sunset-operations-theme",
  "label": "Sunset operations",
  "className": "theme-sunset-operations",
  "description": "Orange and magenta operational status workspace."
});
