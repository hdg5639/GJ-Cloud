"use client";

import { createThemePart } from "../core";

export const GlassAuroraTheme = createThemePart({
  "id": "glass-aurora-theme",
  "label": "Glass aurora",
  "className": "theme-glass-aurora",
  "description": "Translucent panels with aurora accents and soft depth."
});
