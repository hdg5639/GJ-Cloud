"use client";

import { createThemePart } from "../core";

export const TerminalGreenTheme = createThemePart({
  "id": "terminal-green-theme",
  "label": "Terminal green",
  "className": "theme-terminal-green",
  "description": "High-contrast command center with monospace presentation."
});
