"use client";

import { createThemePart } from "../core";

export const BrutalistMonoTheme = createThemePart({
  "id": "brutalist-mono-theme",
  "label": "Brutalist mono",
  "className": "theme-brutalist-mono",
  "description": "Hard borders, bold type, and deliberate high contrast."
});
