"use client";

import { createThemePart } from "../core";

export const OceanDataTheme = createThemePart({
  "id": "ocean-data-theme",
  "label": "Ocean data",
  "className": "theme-ocean-data",
  "description": "Cool cyan analytics and telemetry visualization theme."
});
