"use client";

import { createThemePart } from "../core";

export const CrimsonSecurityTheme = createThemePart({
  "id": "crimson-security-theme",
  "label": "Crimson security",
  "className": "theme-crimson-security",
  "description": "Dark red incident and security operations theme."
});
