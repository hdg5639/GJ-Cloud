"use client";

import { createThemePart } from "../core";

export const EnterpriseBlueTheme = createThemePart({
  "id": "enterprise-blue-theme",
  "label": "Enterprise blue",
  "className": "theme-enterprise-blue",
  "description": "Conservative blue enterprise workspace with dense structure."
});
