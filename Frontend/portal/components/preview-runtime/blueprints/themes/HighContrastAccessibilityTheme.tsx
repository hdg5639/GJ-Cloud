"use client";

import { createThemePart } from "../core";

export const HighContrastAccessibilityTheme = createThemePart({
  "id": "high-contrast-accessibility-theme",
  "label": "High contrast",
  "className": "theme-high-contrast",
  "description": "Accessibility-focused high contrast theme with strong focus states."
});
