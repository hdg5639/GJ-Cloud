"use client";

import { createFeedbackPart } from "../core";

export const IllustratedEmptyState = createFeedbackPart({
  "title": "Nothing here yet",
  "description": "Create the first record to get this workspace started.",
  "style": "empty",
  "actionLabel": "Create"
});
