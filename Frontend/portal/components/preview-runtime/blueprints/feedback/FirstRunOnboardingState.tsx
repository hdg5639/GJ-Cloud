"use client";

import { createFeedbackPart } from "../core";

export const FirstRunOnboardingState = createFeedbackPart({
  "title": "Set up your workspace",
  "description": "Connect the first data source and choose a starting template.",
  "style": "onboarding",
  "actionLabel": "Start setup"
});
