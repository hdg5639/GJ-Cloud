spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql
  data:
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT}
      password: ${REDIS_PASSWORD:}
  servlet:
    multipart:
      max-file-size: 25MB
      max-request-size: 25MB

server:
  port: 8080

auth:
  server-url: ${AUTH_SERVER_URL}
  jwks-cache-ttl: 3600000

vm:
  service-url: ${VM_SERVICE_URL}

ops:
  management-key-encryption-secret: ${OPS_KEY_ENCRYPTION_SECRET}
  terminal-ticket-ttl-seconds: 30
  vm-ssh-username: ${VM_SSH_USERNAME:ubuntu}
  terminal-idle-timeout-minutes: 30
  allowed-origin: ${PORTAL_ORIGIN}
  file-browser-max-edit-size-bytes: 1048576
  file-browser-max-upload-size-bytes: 26214400
  deployment-lock-ttl-seconds: 1800
  deployment-executor:
    core-pool-size: 2
    max-pool-size: 4
    queue-capacity: 20

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false
